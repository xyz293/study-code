using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class intertion : MonoBehaviour
{
    // Start is called before the first frame update
    public Animator a;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
